// import BirdCard from "./BirdCard";
// import birdData from "../data/birds"
// function Main () {
// //  mapping goes 
// //  Array.map

// // because we are doing this in jsx 
// // we do not need to save to a Var
// // when outside of the the return state, you do have to use a Var
//     return (
//         <div>
//            {
//              birdData.birdData.map((contactData, index) => {
//                 // iterate through each city obj
//                 // take each obj out of the Array
//                 // and pass it down as props into a new component
//                 return <BirdCard contactData={contactData} key={index} />
//              })
//            } 
//         </div>
//     )
// }

// export default Main;